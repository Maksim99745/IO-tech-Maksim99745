# 🚀 Деплой Strapi

## Структура проекта

```
/
├── strapi-backend/    ← Strapi CMS
└── (Next.js фронтенд)
```

## 📋 Шаг 1: Экспорт данных с локального Strapi

Перед деплоем экспортируй данные:

```bash
cd strapi-backend
npm run export
```

Создастся файл `export_*.tar.gz` в папке `strapi-backend/`.

## 📋 Шаг 2: Деплой на Render (рекомендуется)

### Настройки на Render:

1. **New → Web Service**
2. Подключи GitHub репозиторий
3. **Root Directory**: `strapi-backend`
4. **Build Command**: `npm install && npm run build`
5. **Start Command**: `npm start`
6. **Node Version**: 20

### Переменные окружения на Render:

```
NODE_ENV=production
HOST=0.0.0.0
PORT=10000
DATABASE_CLIENT=better-sqlite3
```

## 📋 Шаг 3: Импорт данных на продакшн

После деплоя:

1. Зайди в Render Shell (или SSH на сервер)
2. Загрузи файл `export_*.tar.gz` на сервер
3. Выполни:

```bash
cd strapi-backend
npm run import
```

Или через Render Shell:
- Upload файл через Render Dashboard
- В Shell: `npx strapi import`

## 📋 Шаг 4: Настройка фронтенда

В Vercel (Next.js проект) добавь переменные:

```
NEXT_PUBLIC_STRAPI_URL=https://your-strapi.onrender.com
NEXT_PUBLIC_STRAPI_TOKEN=твой_токен_из_strapi_admin
```

## 🔄 Альтернатива: Railway

1. Railway.app → New Project
2. Deploy from GitHub
3. Root Directory: `strapi-backend`
4. Railway автоматически определит настройки

## ✅ Чеклист

- [ ] Экспортировал данные локально (`npm run export`)
- [ ] Задеплоил Strapi на Render/Railway
- [ ] Импортировал данные на продакшн (`npm run import`)
- [ ] Создал API токен в Strapi Admin
- [ ] Настроил переменные в Next.js проекте
- [ ] Проверил работу сайта

## 💡 Важно

- **Данные не пропадут** — используй экспорт/импорт
- **Один репо** — Strapi в `strapi-backend/`, фронт в корне
- **Автоматический деплой** — при push в main оба проекта обновятся
